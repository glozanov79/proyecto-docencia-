# proyecto-docencia

`jefe_general.py` orquesta los dominios registrados en `DOMINIOS` (mismo archivo):

- `ucc/` — activo. 4 cursos registrados en `ucc/config/cursos.json`.
- `unad/` — pendiente de construir.
- `agenda/` — activo. Dos agentes con dependencia estricta (`calendario` → `correo`);
  ver `agenda/README.md`.
